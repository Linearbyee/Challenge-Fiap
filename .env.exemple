FLASK_SECRET_KEY=change-me-super-secret
GEOLocation_API_BASE=https://ipapi.co

# AbuseIPDB (opcional; se vazio, a integração é ignorada)
ABUSEIPDB_API_KEY=

# Anti-abuso por IP
ATTEMPT_WINDOW_MIN=5      # janela deslizante (min)
ATTEMPT_MAX_FAILS=10      # falhas antes do ban
ATTEMPT_BLOCK_MIN=60      # duração do ban (min)

PORT=5000
DEBUG=true
